	ORG	0000H
	SJMP	START
START:
	CALL	INITDATA
	MOV	B, 31H
	MOV	A, 32H
	MUL	AB
	MOV	40H, A
	MOV	R3, B
	MOV	B, 30H
	MOV	A, 32H
	MUL	AB
	MOV	42H, B
	ADD	A, R3
	MOV	41H, A
	MOV	A, 42H
	ADDC	A, #0H
	MOV	42H, A
	SJMP	$

INITDATA:
	MOV	30H, #0FFH
	MOV	31H, #0FFH
	MOV	32H, #0FFH
	RET

	END
