 			ORG	0000H
			SJMP	START

START:	MOV	R2, #0H
			MOV	R3, #10H
			MOV	R4, #20H
			MOV	R5, #5
			CALL	INITBCD
			MOV	DPTR, #0100H
			CALL	ADDBCD
			
ADDBCD:	MOV	DPL, R2
			MOVX	A, @DPTR
			MOV	B, A
			MOV	DPL, R3
 		   MOVX	A, @DPTR
			ADDC	A, B
			DA		A
			MOV	DPL, R4
			MOVX	@DPTR, A
			INC	R2
			INC	R3
			INC	R4
			DJNZ	R5, ADDBCD			
			SJMP	FINISH

INITBCD: MOV	DPTR, #0100H
			MOV	A, #10H
			MOVX	@DPTR, A
			INC	DPL
			MOV	A, #32H
			MOVX	@DPTR, A
			INC	DPL
			MOV	A, #54H
			MOVX	@DPTR, A
			INC	DPL
			MOV	A, #76H
			MOVX	@DPTR, A

			MOV	DPL, #10H
         MOV	A, #78H
			MOVX	@DPTR, A
			INC	DPL
			MOV	A, #56H
			MOVX	@DPTR, A
			INC	DPL
			MOV	A, #34H
			MOVX	@DPTR, A
			INC	DPL
			MOV	A, #92H
			MOVX	@DPTR, A

			CLR	C
			RET

FINISH:	SJMP	$
			END

MOV	DPL, R3
			MOVX  A, @DPTR
			ADDC	A, @DPTR

